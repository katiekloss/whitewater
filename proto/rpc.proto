syntax = "proto3";
package whitewater;

service WhitewaterRpc {
    rpc Set (SetRequest) returns (SetReply);
    rpc Run (stream RaftMessage) returns (stream RaftMessage);
}

message SetRequest {
   string key = 1;
   string value = 2;
}

message SetReply {
}

message RaftMessage {
}